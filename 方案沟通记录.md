# 方案沟通记录

> 本文件记录与用户讨论确认的设计决策与需求变更，供后续开发与维护参考。每次讨论后更新。

## 项目定位
- 网络工程师（H3C）个人使用的 **LLD（Low-Level Design）规划工具**
- 为多个客户项目规划网络端口连接，最终产出 Port_Design 格式 Excel 交付物
- 单人本地使用：Streamlit 本地网页 + `启动.bat` 双击启动，数据存本地 SQLite

## 已确认决策

| # | 决策 | 说明 |
|---|---|---|
| 1 | 不用 NetBox | 数据模型对齐 NetBox 概念（DeviceType / Device / Interface / Cable），但自建轻量应用 |
| 2 | Cable 连接模型 | 一条物理链路 = `project_connections` 一行（port_a < port_b 规范化），不存双向两行；"自动反向" = 从任一设备视角可见 |
| 3 | 原生 sqlite3 | 不用 SQLAlchemy；9 张表（模板/槽位/端口/项目/设备实例/端口实例/连接 + 全局选项字典 + 项目 VLAN 字典） |
| 4 | 服务器是正式设备类型 | 模板库支持 server 类型（扁平端口列表），加入项目、下拉选端口、导出出 sheet |
| 5 | 选项值不硬编码 | 接口状态/聚合模式/接口模式 = 全局字典（页面 05）；VLAN = 项目级字典（项目详情「VLAN 设置」）；导入/录入遇新值自动扩充 |
| 6 | 导出格式 | 每台设备一个 sheet 的 Port_Design 格式，槽位并入接口列（`Slot2_Fi_10GE_41`），核心列保留 |
| 7 | Excel 导入 | 两阶段解析（先收集设备/端口计划再建连接），镜像行合并，模板库随导入自动扩充，脏数据如实警告 |
| 8 | 聚合组按端存储 | `aggregation_group_a/b`：每台设备聚合组是本地编号，各端管各端 |
| 9 | 连接录入交互（2026-08-02 定稿） | **设备平铺选择（segmented_control）→ 筛选（仅显示UP/显示All）→ 单击行高亮 →「✏️ 编辑所选行」弹窗**；保存即生效（单行同步引擎 sync_device_rows） |
| 10 | 双击/格内编辑 | Streamlit 框架不支持（无双击事件、data_editor 单元格编辑不可靠）→ **放弃格内编辑**，采用行高亮+弹窗 |
| 11 | 框架评估 | 曾评估 PySide6 桌面重写（已完成 V1.0 但用户选择放弃）与 streamlit-aggrid（兼容性风险，实测编辑失效）→ **最终确定继续 Streamlit** |
| 12 | 表格高度 | 1.60 的 auto 高度固定 400px → 按行数动态设置 `height=40+35*行数` |
| 13 | 规模评估 | ≤100 种设备类型、≤50 个项目 → SQLite 毫秒级，无需优化 |
| 14 | 依赖安装 | 本机 pypi.org SSL 不通，pip 需用清华镜像 `-i https://pypi.tuna.tsinghua.edu.cn/simple` |
| 15 | 设备编辑（2026-08-03） | 底部按钮区「✏️ 编辑设备」（作用于当前选中设备，与删除设备对称），弹窗改名称+位置/机柜，`update_project_device` 保存；同项目设备名唯一校验（添加/编辑共用，编辑排除自己）；**不支持更换模板**（换模板=删设备重加）；改设备名不影响已连好的线（连接按端口 ID 引用） |
| 16 | 设备排序（2026-08-03） | 不做真拖拽（Streamlit 无原生组件，第三方 streamlit-sortables 有 aggrid 前车之鉴）；**最终交互（用户改稿）**：侧边栏「➕ 添加设备」按钮下方一组「⬆ 上移 / ⬇ 下移」，作用于**当前选中设备**（未选中或到边界置灰），点击即生效并刷新——不放每台设备下方（太丑）；数据层 `project_devices.sort_order` 列（init_db 幂等迁移 `_migrate_device_sort_order`，初始按 id 填充），`get_project_devices` 按 `COALESCE(sort_order, id)` 排序（新设备自动排末尾），`reorder_project_devices(project_id, ordered_ids)` 重写编号；**导出 sheet 顺序同步受影响**（同一数据源） |
| 17 | 模板导入分组显示（2026-08-03） | 导入弹窗预览改为**分组卡片**：🟢「✅ 可以导入（N 个）」表格（型号\|端口数）+ 🟠「⏭ 跳过（K 个）」表格（名称\|原因）；跳过原因：库中已存在（不覆盖）/ 信息表未定义 / 信息表类型无效 / 空 sheet / 无「接口」表头 / **信息表已定义但文件无对应 sheet（新增提示）**；无可导入时确认按钮置灰。核心：新增 `classify_templates(source)`（预览与导入**共用同一分类标准**，杜绝显示与实际不一致），`import_templates` 重构为消费分类结果，`preview_templates` 已删除，`_parse_library_sheets` 的 skipped 改结构化（name+reason） |
| 22 | 对端固定端口下拉（2026-08-03） | 批量连线 A 模式（多台×固定端口）对端端口名从手输改为**下拉选择所选设备共同拥有且未占用的端口**（`pu.common_device_ports(project_id, dev_names)` 取模板端口+自由端口实例的交集并过滤 `is_used=0`，自然序）；无交集/未选设备时降级手动输入并提示 |
| 30 | 新增设备弹窗改 data_editor 批量添加（2026-08-04） | 用户发现新增设备弹窗适合 data_editor（文本字段无联动需求）→ 改造：表格 `num_rows='dynamic'` 每行一台设备，列 = 模板（SelectboxColumn 下拉，required）| 设备名称 | 位置/机架 | 管理IP | BMC IP；底部「➕ 添加行」原生加行；保存按钮「添加设备（N 台）」**整批预检**（名称空/库内重名/批内重名/模板无效 → 全部不添加列出行号错误），全过才逐行 `add_project_device`（模板按 model 反查 id）；编辑数据存 `session_state['add_dev_data']` 每轮存回，成功清状态关弹窗 |
| 29 | 批量编辑行实验失败已回退（2026-08-04） | 「✏️ 批量编辑行」实验（data_editor 表格双击编辑连接行）**测试失败已整体回退**（删按钮+函数，恢复 7 列按钮区）。**结论：data_editor 行级联动确认为框架硬限制**——下拉选项列级共享、点击下拉不触发事件（只有选完值才触发），无法做"选设备后该行端口下拉联动"；试过 动态并集下拉/最近编辑行跟随/键入过滤/端口优先解析 均达不到行级联动体验。后续 UI 设计不再尝试行级联动；批量编辑设备（data_editor 单设备参数编辑）不受影响保留 |
| 28 | 批量编辑改用 data_editor 双击编辑（2026-08-04） | 用户要求试 data_editor 格内编辑——**实测可用**（推翻决策 #10/#11「格内编辑不可靠」旧结论，Streamlit 升级后已修复）。批量编辑设备弹窗重写：`st.data_editor` 一张表（设备名称/设备位置/管理IP/BMC IP 可编辑，模板/端口占用 `disabled` 只读），**双击单元格直接改**；保存按钮显示「💾 保存修改（N 台）」（N=对比编辑前后差异行，不依赖编辑事件），逐行 `update_project_device`（名称空/重名报错不保存该行），全部成功清 `de_dev_grid` 状态重读库刷新。**数据差异检测用 `_cell(v)`（`'' if v is None or pd.isna(v) else str(v)`）防 NaN**。AppTest 不支持 data_editor 编辑模拟，只能真机验证 |
| 27 | 批量编辑设备（2026-08-04） | 底部按钮区新增「✏️ 批量编辑设备」**独立弹窗**（width='large'，设备预览保持只读）：全部设备一张表（设备/模板/位置/管理IP/BMC IP/端口占用）**单击行选中**（selection_mode='single-row'，与连接网格同套读取逻辑）→ 下方表单预填（名称/位置/管理IP/BMC IP，bd_ 前缀 key）→ 💾 保存走 `update_project_device`（**重名校验**复用 ValueError）→ **行内 st.success/st.error 不关弹窗**（与模板管理弹窗同模式），保存后清选中继续下一条；表格高度 40+35×min(设备数,12) 滚动。**用户确认后删除「✏️ 编辑设备」按钮及 `edit_device_dialog` 函数**（批量编辑功能完全覆盖，入口统一） |
| 26 | IP 覆盖新增+设备预览（2026-08-04） | `add_project_device` 加 `mgmt_ip`/`bmc_ip` 参数（INSERT 带两列；不带 IP 存空串，读取按 falsy 处理与 NULL 等价）；「➕ 添加设备」弹窗加「管理IP」「BMC IP」输入框（两列并排选填，与编辑弹窗一致）；「📋 设备模板预览」按钮改名「📋 设备预览」，弹窗表格加 管理IP/BMC IP 列（空值显示 `-`）。AppTest 坑：底部按钮区在未选中设备时被 `st.stop()` 跳过，测试需先点击侧边栏设备按钮再验证底部按钮 |
| 25 | 线缆标签按设备遍历（2026-08-04） | 用户纠正：**单 sheet**、两列 From\|To（不加设备列），**每根线缆两端各一行**（本端恒在 From、对端在 To）——两端标签都补齐（之前只有 A 端视角一行）；**实现方式用户明确要求直接遍历设备+端口**（"你已经知道了所有的设备和信息，一台一台整合，不需要做太多操作"）：`export_cable_labels` 遍历 `get_project_devices` → `get_device_ports` → `get_connection_by_port` 取连接（无连接跳过），本端在 From、对端在 To（对端位置 dev_info 查）；每根线缆在两端设备各出现一次；每行 = 该设备一个连接端口，设备块顺序 = 侧边栏设备顺序、组内端口顺序 = 模板端口顺序；删除了"拆两行+排序键"实现与 `_cable_rows`。测试教训：设备名可含 `-`（Ser-Ctrl-01），测试解析 From 要用 `endswith(设备名-端口)` 长名优先，不能按 `split('-')` 位置取段 |
| 24 | 标签导出优化（2026-08-04） | 顶部拆「🏷 导出设备标签」「🏷 导出线缆标签」**两个独立按钮**各自弹窗（顶部 7 列按钮区）；线缆标签弹窗**删除「带位置/不带位置」快捷按钮**（格式仍可手输 + 实时预览）；设备标签**列顺序可调**：导出列顺序 = 勾选列列表顺序（新勾选加入末尾 / 取消移除），**下拉选列 + ⬆上移/⬇下移**换位（与设备排序一致交互），调序后记住（存 app_settings，`device_label_columns` 即有序勾选列表）；`export_device_labels` 按传入列顺序导出（过滤不再按固定键序） |
| 23 | 设备IP+标签导出（2026-08-04） | `project_devices` 加 `management_ip`/`bmc_ip`（迁移 `_migrate_device_ip_fields` 幂等；「✏️ 编辑设备」弹窗新增两字段，`update_project_device` 扩参）。**设备标签导出**：**全部设备**（含未连接，用户明确），**多列勾选**（设备名称/设备位置/设备型号/管理IP/BMC IP，默认全勾，勾选状态 JSON 存 app_settings）。**线缆标签导出**：只含已连接线缆，列头 `From \| To`（From=链路 A 端 / To=B 端，两端同一格式串），格式串占位符 `{位置} {设备} {接口}`（可自定义），**空值段连同其后分隔符自动删除**（`{位置}-{设备}-{接口}` 位置空 → `SW1-Fi_1`；`render_label` 纯函数）；预设「带位置/不带位置」快捷按钮，实时预览前 5 条；行排序**按本端（A 端）设备顺序+端口自然序**。**两个文件分别下载**（`项目名_设备标签.xlsx` / `项目名_线缆标签.xlsx`）。设置存新表 `app_settings`（SCHEMA 表 10，键值对），下载时保存。弹窗预览与导出共用 `_cable_row`/`cable_label_preview`（同源渲染防不一致） |
| 21 | 批量连线聚合组逐口（2026-08-03） | 批量连线弹窗**本端+对端聚合组**都支持**空格分隔多值**（用户方案，未做 data_editor 表格编辑——用户选择直接空格分隔）：空→全空；1 值→套全部；N 值（空格）→逐口对应，N 须=端口数，数量不符弹窗报错不保存。拆分纯函数 `pu.split_agg_values(raw, n)`（utils/project_utils.py，可测）；`_param_block` 加 agg_placeholder 参数。**提示词示例用纯数字（如 11）不用 Agg- 前缀**（用户实际输入纯数字聚合组号） |
| 20 | 批量连线（2026-08-03） | 底部按钮区「⚡ 批量连线」弹窗（编辑所选行旁）：本端端口多选（未占用）→ 对端模式 A「多台设备 × 固定端口」（选 N 台设备 + 填一个端口名如 NIC01，口1↔设备1 按列表顺序）或 B「单台 × 多端口」（选 1 台设备 + N 个端口）→ 数量一致才可连线 → 参数左右对称一套套全部（复用 `_param_block`/`dict_field`/`vlan_multiselect` 模块级函数，联动保留）→ **整批回滚**：`precheck_batch_rows` 只读预检（本端/对端占用、模板设备缺端口笔误、链路重复），任一条失败全部不保存并列出原因；通过后 `sync_device_rows` 多行一次创建。底层引擎本就支持多行，仅新增预检函数 + 弹窗 |
| 19 | 公共参数按端拆分+布局联动（2026-08-03） | 用户发现「公共参数」（聚合模式/接口模式/VLAN）也是单列 → 拆成 `aggregation_mode_a/b`、`interface_mode_a/b`、`vlan_id_a/b`（迁移 `_migrate_split_common_params` 重建表，**旧值两端复制**——用户决策，避免对端空白）。弹窗删除「公共参数」横线区，每侧布局：接口状态→接口模式→VLAN→聚合模式→聚合组→连接描述→备注。**联动（用户确认）**：聚合模式=`/`（字典值，不聚合）→ 聚合组默认显示 `/`（存 `/`）；接口模式=`Route` → VLAN 默认显示 `/`（未选存 `/`）；**可手动改、不禁用、原值保留切回恢复**。`/` 不进全局/项目字典（VLAN 项目字典、interface_status 字典、aggregation/interface 模式字典都过滤）。导入镜像行、导出各端按端；网格显示本端值 |
| 18 | 描述/备注按端存储（2026-08-03） | 用户发现「连接描述左右各填一份实际被合并成一列」是 bug → **描述/备注都拆成按端独立**（与聚合组一致）：`description_a/b`、`note_a/b`（init_db 幂等迁移 `_migrate_split_desc_note` 重建表，旧值归 `_a`）。编辑弹窗左右各「连接描述（本端/对端，留空自动生成）」+「备注（本端/对端）」；**自动生成规则（用户确认）：描述两端都生成**（本端 `To-对端设备_端口`、对端 `To-本端设备_端口`），**备注不自动生成默认留空**（用户手填）；保存不再「以改动侧为准」。导入镜像行两端描述/备注按端各自保留（不互相覆盖）；导出各端 sheet 写各端值；网格列显示本端值。端口状态本就每端口独立（project_ports.interface_status），无需处理。**迁移后遗留（2026-08-03）**：旧连接对端描述空白（28/40 条），用户选择**一次性批量重写**全部 40 条连接两端描述 = `To-对端设备_对端端口`（无条件覆盖含手工值，用户明确要求；备注不碰）；一次性脚本执行后已删，**不进 init_db**（避免反复覆盖用户后续修改） |

## 已知问题与规避
- Streamlit 热重载只对页面脚本生效，`utils/` 等被 import 的模块改动需**重启应用**才生效
- 顶部导航栏（stHeader）与侧边栏保持默认，不做隐藏（此前尝试引发遮挡/闪退问题）

## 遗留待办 / 可能扩展
- （空）用户提出新需求时在此登记
